services:
  - type: web
    name: smart-wardrobe-control
    runtime: python
    plan: free
    rootDir: wardrobe_web
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --bind 0.0.0.0:$PORT --workers 1 --threads 8
    autoDeploy: true
